// Simple, remove the spaces from the string, then return the resultant string.

//  regex i
function noSpace(x){
    return x.replace(/ /g, '')
  }

  function noSpace(x){
    return x.replace(/\s/g, '')
   }


//    split join methods

function noSpace(x){
    return x.split(' ').join('');
  }